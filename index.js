function theBeatlesPlay(musicians, instruments) {
  var array = []
  if(length.musicians > 0 && length.musicians >= length.instruments)
  for (i = 0; i < length.musicians; i++) {
    array.push('${musicians[i]} plays ${instruments[i]}')
  }
  elseif(length.instruments > 0 && length.instruments >= length.musicians)
  for (i=0; i < length.instruments; i++) {
    array.push('${musicians[i]} plays ${instruments[i]}')
  }
  return array
}
function johnLennonFacts(factsArray) {

}
function iLoveTheBeatles(n) {
  var emphaticArray = []
}
